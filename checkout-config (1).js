// ============================================================================
// CHECKOUT CONFIG
//
// Fill in your Supabase project URL and anon (public) key here. Both are
// safe to expose in client-side code — see docs/SETUP.md for where to find
// them in your Supabase dashboard (Settings > API).
//
// Until these are filled in with real values, checkout-integration.js will
// log an error to the browser console and the site's original (non-backend)
// checkout behavior continues to work exactly as it did before.
// ============================================================================

window.CHECKOUT_SUPABASE_CONFIG = {
  url: "YOUR_SUPABASE_PROJECT_URL", // e.g. "https://xxxxxxxxxxxx.supabase.co"
  anonKey: "YOUR_SUPABASE_ANON_KEY",
};
